process {
    withName: 'RUN_BOLTZ' {
        ext.args = '--seed 0'
        containerOptions = {
            workflow.containerEngine in ['singularity', 'apptainer'] ?
            '--nv -e TORCHINDUCTOR_CACHE_DIR=.torchinductor' :
            workflow.containerEngine == 'docker' ?
            '--gpus all -e TORCHINDUCTOR_CACHE_DIR=.torchinductor' :
            ''
        }
    }
}
